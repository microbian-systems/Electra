@* @model Electra.Web.BlogEngine.Models.ArticleViewModel *@
@model dynamic

@{
    Layout = "_Layout";
    ViewData["Title"] = "Article"; // Model.Title
}

<div class="container mx-auto px-4 py-8">
    <article class="prose lg:prose-xl mx-auto">
        <h1>Title Placeholder</h1>
        <div class="text-gray-600 mb-4">
            <span>By Author</span> | <span>Date</span>
        </div>
        
        @*
        @if (!string.IsNullOrEmpty(Model.ThumbnailUrl))
        {
            <img src="@Model.ThumbnailUrl" alt="@Model.Title" class="w-full h-64 object-cover rounded-lg mb-8" />
        }
        *@

        <div class="content">
            Content Placeholder
        </div>

        <div class="mt-8 pt-8 border-t border-gray-200">
             @* Partial for related articles will go here *@
             @* @await Component.InvokeAsync("RelatedArticles", new { articleId = Model.Id, tags = Model.Tags }) *@
        </div>
    </article>
</div>